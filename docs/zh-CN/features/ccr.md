---
title: Claude Code Router (CCR)
---

# Claude Code Router (CCR)

[CCR](https://github.com/musistudio/claude-code-router/blob/main/README_zh.md)（Claude Code Router）是一个强大的代理路由器，可以实现多个 AI 模型的智能路由和成本优化。ZCF 内置完整的 CCR 管理能力，帮助您快速搭建高可用的 Claude Code 代理系统。

## 什么是 CCR

CCR 是一个 AI 模型代理路由器，主要功能包括：

- 🆓 **免费模型接入**：通过 Claude Code 界面使用免费 AI 模型（如 Gemini、DeepSeek）
- 🎯 **自定义路由**：根据规则将不同类型的请求路由到不同的模型
- 💰 **成本优化**：通过为不同任务使用合适的模型，显著降低 API 成本
- 🔄 **自动切换**：智能选择最适合的模型处理不同任务
- 📊 **使用统计**：详细的使用量统计和成本分析

## 核心优势

### 成本优化

通过智能路由，为不同任务选择最经济的模型：

- **简单任务** → 使用免费模型（Gemini、DeepSeek）
- **复杂任务** → 使用高性能模型（Claude Opus、GPT-4）
- **快速任务** → 使用快速模型（Claude Haiku、GPT-3.5）

> 💡 **成本节省**：合理使用 CCR 可以将 API 成本降低 50-80%

### 模型多样性

CCR 支持接入多种 AI 模型：

- **Claude 系列**：Opus、Sonnet、Haiku
- **OpenAI 系列**：GPT-4、GPT-3.5、GPT-4 Turbo
- **免费模型**：Gemini、DeepSeek、Moonshot
- **自定义模型**：支持任何兼容的 API 端点

### 高可用性

- **自动故障转移**：主模型不可用时自动切换到备用模型
- **负载均衡**：在多个模型间分配请求
- **健康检查**：自动检测模型可用性

## 快速开始

### 方式一：通过 ZCF 菜单（推荐）

```bash
npx zcf ccr
```

进入 CCR 管理菜单后，可以选择：

```
CCR 管理菜单
1. 初始化 CCR - 安装并配置 CCR
2. 启动 UI - 启动 CCR Web 界面
3. 服务控制 - 启动/停止/重启 CCR 服务
4. 检查状态 - 查看当前 CCR 服务状态
Q. 退出
```

### 方式二：初始化时直接启用

```bash
npx zcf i -s -t ccr_proxy
```

或在交互式初始化中选择：
```
? 选择 API 认证方式
  ❯ 配置 CCR 代理（Claude Code Router）
```

### 方式三：从 API Key 模式切换

```bash
npx zcf
# 选择 3. 配置 API
# 选择"切换到 CCR 代理"
```

## 安装与配置

### 自动安装

ZCF 会自动检测并安装 CCR：

```bash
npx zcf ccr
# 选择 1. 初始化 CCR
```

ZCF 会：
1. ✅ 检测是否已安装 `@musistudio/claude-code-router`
2. ✅ 自动安装或升级到最新版本
3. ✅ 创建默认配置文件
4. ✅ 配置 Claude Code 使用 CCR 代理

### 配置向导

初始化 CCR 时，ZCF 会引导您完成配置：

1. **选择提供商预设**：支持多个预设提供商，简化配置
2. **配置端口**：默认使用 3456 端口（可自定义）
3. **设置密钥**：配置 CCR 访问密钥
4. **创建备份**：自动备份现有配置

### 配置文件位置

CCR 配置文件保存在：

- **配置目录**：`~/.claude-code-router/`
- **配置文件**：`~/.claude-code-router/config.json`
- **备份目录**：`~/.claude-code-router/`（备份文件格式：`config.json.YYYY-MM-DDTHH-mm-ss-SSSZ.bak`）

## 使用 CCR Web UI

CCR 提供 Web 界面进行高级配置：

```bash
npx zcf ccr
# 选择 2. 启动 UI
```

Web UI 功能包括：
- 📊 实时使用统计
- ⚙️ 路由规则配置
- 🔧 模型管理
- 📈 成本分析
- 🔄 服务控制

访问地址：`http://localhost:3456/ui`（默认端口）

## 服务管理

### 启动服务

```bash
npx zcf ccr
# 选择 3. 服务控制 → 启动服务
```

或手动启动：
```bash
ccr start
```

### 停止服务

```bash
npx zcf ccr
# 选择 3. 服务控制 → 停止服务
```

或手动停止：
```bash
ccr stop
```

### 检查状态

```bash
npx zcf ccr
# 选择 4. 检查状态
```

或手动检查：
```bash
ccr status
```

## 路由规则配置

CCR 支持灵活的路由规则配置，可以通过 Web UI 或配置文件设置。配置文件使用 JSON 格式，主要包含 `Providers` 和 `Router` 两部分。

### 完整配置示例

```json
{
  "LOG": true,
  "CLAUDE_PATH": "",
  "HOST": "127.0.0.1",
  "PORT": 3456,
  "APIKEY": "sk-zcf-x-ccr",
  "API_TIMEOUT_MS": "600000",
  "PROXY_URL": "",
  "transformers": [],
  "Providers": [
    {
      "name": "gemini",
      "api_base_url": "https://generativelanguage.googleapis.com/v1beta",
      "api_key": "sk-free",
      "models": ["gemini-pro", "gemini-pro-vision"]
    },
    {
      "name": "claude",
      "api_base_url": "https://api.anthropic.com/v1",
      "api_key": "sk-ant-xxx",
      "models": ["claude-sonnet-4-5", "claude-opus-4-1"]
    }
  ],
  "Router": {
    "default": "gemini,gemini-pro",
    "background": "gemini,gemini-pro",
    "think": "claude,claude-sonnet-4-5",
    "longContext": "claude,claude-opus-4-1",
    "longContextThreshold": 60000,
    "webSearch": "gemini,gemini-pro"
  }
}
```

### 配置字段说明

#### 基础配置

| 字段 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| `LOG` | boolean | 是否启用日志 | `true` |
| `HOST` | string | 服务监听地址 | `127.0.0.1` |
| `PORT` | number | 服务端口 | `3456` |
| `APIKEY` | string | CCR API 密钥 | `sk-zcf-x-ccr` |
| `API_TIMEOUT_MS` | string | API 超时时间（毫秒） | `600000` |
| `PROXY_URL` | string | 代理 URL（可选） | `""` |

#### Providers 配置

`Providers` 是一个数组，每个 Provider 包含：

| 字段 | 类型 | 说明 |
|------|------|------|
| `name` | string | 提供商名称（用于路由规则） |
| `api_base_url` | string | API 基础 URL |
| `api_key` | string | API 密钥（免费模型可使用 `sk-free`） |
| `models` | string[] | 该提供商支持的模型列表 |
| `transformer` | object | 可选的请求转换器（用于 API 兼容性） |

#### Router 配置

`Router` 定义了不同场景下的模型路由规则，格式为：`${providerName},${modelName}`

| 字段 | 类型 | 说明 |
|------|------|------|
| `default` | string | 默认路由（格式：`provider,model`） |
| `background` | string | 后台任务路由（可选） |
| `think` | string | 思考任务路由（可选） |
| `longContext` | string | 长上下文任务路由（可选） |
| `longContextThreshold` | number | 长上下文的 token 阈值（可选） |
| `webSearch` | string | 网页搜索任务路由（可选） |

### 路由策略示例

#### 成本优化配置

为不同场景选择最经济的模型：

```json
{
  "Router": {
    "default": "gemini,gemini-pro",           // 默认使用免费模型
    "think": "gemini,gemini-pro",             // 思考任务也用免费模型
    "longContext": "claude,claude-sonnet-4-5", // 长上下文用中等模型
    "longContextThreshold": 60000              // 超过 60k tokens 使用长上下文路由
  }
}
```

#### 性能优先配置

为不同任务选择最适合的高性能模型：

```json
{
  "Router": {
    "default": "claude,claude-opus-4-1",        // 默认使用最强模型
    "background": "claude,claude-haiku-4-5",   // 后台任务用快速模型
    "think": "claude,claude-sonnet-4-5",      // 思考任务用平衡模型
    "longContext": "claude,claude-opus-4-1"      // 长上下文用最强模型
  }
}
```

#### 混合配置

结合免费模型和付费模型：

```json
{
  "Providers": [
    {
      "name": "gemini",
      "api_base_url": "https://generativelanguage.googleapis.com/v1beta",
      "api_key": "sk-free",
      "models": ["gemini-pro"]
    },
    {
      "name": "claude",
      "api_base_url": "https://api.anthropic.com/v1",
      "api_key": "sk-ant-xxx",
      "models": ["claude-sonnet-4-5", "claude-opus-4-1"]
    }
  ],
  "Router": {
    "default": "gemini,gemini-pro",           // 简单任务用免费模型
    "think": "claude,claude-sonnet-4-5",      // 复杂思考用 Claude
    "longContext": "claude,claude-opus-4-1"     // 长上下文用最强模型
  }
}
```

## 提供商预设

ZCF 支持多个 CCR 提供商预设，简化配置流程：

```bash
npx zcf ccr
# 选择 1. 初始化 CCR
# 选择提供商预设
```

支持的预设包括：
- **302.AI**：企业级 AI 服务
- **GLM**：智谱 AI
- **MiniMax**：MiniMax AI 服务
- **自定义**：配置自定义提供商

## 监控与分析

### 使用量统计

结合 `ccu` 命令查看 API 使用情况：

```bash
npx zcf ccu
```

显示信息包括：
- 📊 总请求数
- 💰 总成本
- 📈 每日使用量趋势
- 🎯 各模型使用分布

### CCR 内置统计

通过 CCR Web UI 可以查看：
- 实时请求监控
- 各模型使用情况
- 成本分析报告
- 性能指标

## 与 Claude Code 集成

### 自动配置

CCR 设置完成后，ZCF 会自动配置 Claude Code 使用 CCR 作为 API 代理：

- ✅ 更新 `~/.claude/settings.json`
- ✅ 设置 API 类型为 `ccr_proxy`
- ✅ 配置代理地址和端口

### 切换回直接 API

如果需要切换回直接 API 模式：

```bash
npx zcf
# 选择 3. 配置 API
# 选择"使用 API Key"或"使用官方登录"
```

## 常见问题

### Q: CCR 服务启动失败？

**A**: 检查以下项：
1. 端口是否被占用：`lsof -i :3456`（macOS/Linux）或 `netstat -ano | findstr :3456`（Windows）
2. 配置文件格式是否正确：检查 `~/.claude-code-router/config.json` 的 JSON 格式
3. 依赖是否完整安装：确认 `@musistudio/claude-code-router` 已正确安装
4. 查看日志文件（如果启用了 LOG）：`~/.claude-code-router/logs/`

### Q: 如何配置多个模型？

**A**: 在 `Providers` 数组中添加多个提供商配置，然后在 `Router` 中指定不同场景使用的模型。例如：

```json
{
  "Providers": [
    {
      "name": "gemini",
      "api_base_url": "https://generativelanguage.googleapis.com/v1beta",
      "api_key": "sk-free",
      "models": ["gemini-pro"]
    },
    {
      "name": "claude",
      "api_base_url": "https://api.anthropic.com/v1",
      "api_key": "sk-ant-xxx",
      "models": ["claude-sonnet-4-5"]
    }
  ],
  "Router": {
    "default": "gemini,gemini-pro",
    "think": "claude,claude-sonnet-4-5"
  }
}
```

### Q: CCR 影响性能吗？

**A**: CCR 本身性能开销极低（<5ms），主要影响来自网络延迟。使用本地部署的模型可以显著降低延迟。

### Q: 如何监控 CCR 状态？

**A**: 
```bash
npx zcf ccr
# 选择 4. 检查状态
```

或访问 Web UI：`http://localhost:3456/ui`

## 最佳实践

1. **先配置基础 API**：建议先完成常规 API 配置，再切换到 CCR，确保有回退路径
2. **合理配置路由**：根据任务类型配置路由规则，平衡成本和性能
   - 简单任务 → 免费模型（Gemini、DeepSeek）
   - 复杂任务 → 高性能模型（Claude Opus、GPT-4）
   - 长上下文 → 专用长上下文模型
3. **使用 Web UI 管理**：复杂配置建议使用 CCR Web UI（`http://localhost:3456/ui`）进行可视化配置
4. **定期监控**：使用 `npx zcf ccu` 定期查看使用量，及时调整配置
5. **备份配置**：每次修改前 ZCF 会自动备份，也可手动备份 `~/.claude-code-router/config.json`
6. **测试路由**：配置完成后，在不同场景下测试路由是否按预期工作

## 版本兼容性

> ⚠️ **v2.9.1 版本用户重要提示**：如果您之前使用过 ZCF v2.9.1 版本初始化 CCR，请重新执行 CCR 初始化流程，以确保安装正确的 `@musistudio/claude-code-router` 包。v2.9.1 版本中存在包名错误问题，该问题已在后续版本中修复。

## 自动更新

ZCF 支持自动检查和更新 CCR：

```bash
npx zcf check-updates
```

或通过菜单：
```bash
npx zcf
# 选择 +. 检查更新
```

## 下一步

了解更多 CCR 相关内容：

- 📊 [使用分析 ccu](../cli/ccu.md) - 了解如何查看 API 使用情况
- 🔧 [配置管理](../advanced/configuration.md) - 深入学习配置管理
- 🎯 [Claude Code 配置](claude-code.md) - 了解 Claude Code 与 CCR 的集成
- 📚 [CCR 官方文档](https://github.com/musistudio/claude-code-router/blob/main/README_zh.md) - 查看 CCR 详细文档
